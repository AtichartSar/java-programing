{
  "status": 200,
  "message": "Get Report Selling successful",
  "entries": [
    {
      "dateTime": 1569897102,
      "list": [
        {
          "title": "บริษัท ก.",
          "amount": 50000
        },
        {
          "title": "บริษัท ข.",
          "amount": 9000
        },
        {
          "title": "บริษัท ค.",
          "amount": 12000
        }
      ]
    },
    {
      "dateTime": 1572575502,
      "list": [
        {
          "title": "บริษัท ข.",
          "amount": 40000
        },
        {
          "title": "บริษัท ค.",
          "amount": 10000
        }
      ]
    }
  ]
}




JSONArray list = new JSONArray();
        JSONObject entries = new JSONObject();
        JSONArray listfinal=new JSONArray();
        long ti=0;
        for (int i=0;i<arr.size();i++){
            ti=arr.get(i).getDateTime();
           if(i>0){
               if (arr.get(i-1).getDateTime()!=ti){
                   entries.put("list",list).put("dateTime",arr.get(i).getDateTime());
                   listfinal.put(entries);
                   System.out.println("h"+i);
                   list = new JSONArray();
                   entries=new JSONObject();

               }
           }
           JSONObject obj=new JSONObject();
           obj.put("title",arr.get(i).getTitle()).put("amount",arr.get(i).getAmount());
            list.put(obj);
            System.out.println(i+" "+obj);
            if(i==arr.size()-1){
                System.out.println("hello"+i);
                entries.put("list",list).put("dateTime",arr.get(i).getDateTime());
                listfinal.put(entries);
            }
        }


